CRA----create react App
many people creates projects with CRA
->slow hot reloading
->uneccessary configuration
->slow startup
->difficult configuration
#command
npx create-react -app my-app


//Vite-->
1.extremely fast
2.HMR -->HOT module Replacement

                ctrl+s
                   |
                browser updates immediately
                    |
                no full page reloading
3.Lightweight:
only essential packages will be installed so it is called Lightweight
4.Modern technologies:
uses ES Modules during development 
5.Easy configuration

Dashboard:
<h1>250/h1> -->static
250 students....might
    /          \
    Increase    decrease
javascript knows
butreact doesnt know
react get to know that the data changed
-->state

real world problem:
Facebook likes--100
        |
    if someone likes it
        |
        101(react state)
what is state?
State is a buil-in react object that stores the data which can change over time
when the state changes,react automatically updates the UI

->Variable changes
    |
    but react doesnt know
    |
    solution:
    |
    state
     |
     react re-renders
     |
     UI updates
state changes:
    ||
    react dectects
    ||
    components re-renders
    |
    new displayed
heart of react 

what is hook?
A hook is a special react function that allows functional component 
to use react feactures

-->useState is a hook used to create and manage 
state inside a functional components

import{useState} from "react"

syntax of useState:
const [state,setState] = useState(initial value)//250
        |           |
    current value   it is a function to update value
    
    //Tv
state = 20
setState = 21 22 23

Events:action performed by the user
1.clicking a button
2.typing in a text box
3.submitting a form 
4.hovering on a element
5.pressing a key

react --event handlers

common react Events
1.onclick-->ButtonClicks
2.onChange-->input value changes
3.onSubmit-->form submission
4.onMouseEnter-->Mouse enters an element
5.on Mouseleave-->mouse leaves the element
onkeyDown-->key pressed
onKeyUp-->key is released

Controlled components?
is a form elements whose values is controlled by react state.

Two way data Binding?
    user types
        |
     Input box
        |
    onChange
        |
    react state
        |
        value
        |
    Input box updated

Data Moves:
input-->state
state-->input two way data binding 






10/7/26
Register.jsx--->registration form and student table
         /         \
Register.jsx        StudentTable
    |                    |
arra[]               array[]->
    \                   / 
          App.jsx -- array[declared in App.jsx]
               By using (Props)


(declared in registration.jsx) 


What is routing?
Routing is the process of dispalying different pages based on the URL without refreshing the entire application 


SPA ->single page application

How many HTML pages does react load?
only one that is index.html 



One HTML page multiple page  comopenets

MPA-->Multi page application  (every page loads)


What is react router?
It is a library that enables navigations between pages without reloading the browser


React -- No Built in routing

We install -- react-router-dom

 npm install react-router-dom 

 Routes -- hook 
 are collection of all the url paths in your application 



 Morning:
 separation of table
 React-router-dom-install
 main.jsx -->wrapping of app
 Link,NavLink<Rotes,Route 





 USE EFFECT: 
ex: when open ints ..
-notifcation,story,post,are loaded

Email : Loads the emails


Open clg websites :





DEFFINITION FOR USE EFFECT :

*use effet is a react hook used to peform sideffects after a componenet renders 
                   or
use effect is automatically run when the component is loaded 



LIFE CYCLE OF USE EFFECT() :

                      Human -->Death
                      /    \
                 Birth    LIFE

      |
      (remove)Unmount <- React -->Mount(create)
                          |
                        update

-Mount : first time the component appears 
    for ex: Dashboard opens

-Update : state changes 
    ex:adding a student 
- Unmount : component removed 
    ex : Logout ,Dashboard disappears


Dependency array :
count-1
useEffect(()=>{
    console.log("Running")
});
-->Runs at evry render
count-2
useEffect(()=>{
    console.log("Running)
}[]);
--->runs only once 

count-3
useEffect(()=>{
    console.log("Running")
},[count]);

ex:Time is the best example   

Local Storage:
browsers features that stores data permanently until it is removed  
1.Saves data
localStorage.setItem("isLogged","true")
                           |         |
                           key       value 
            
2.REad Data:
localStorage.getItem("isLoggedIn")

3.Remove Data:
localStorage.removeItem("isLoggedIn")


4.clear Everything:
localStorage.clear()

